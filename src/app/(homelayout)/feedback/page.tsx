import FeedBack, {
  ContactInputBox,
  ContactTextArea,
} from "@/components/feedback/FeedBack";

const page = () => {
  return (
    <div className="mx-auto common">
      <FeedBack />
    </div>
  );
};

export default page;
